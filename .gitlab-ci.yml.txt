stages:
  - build
  - test

build:
  stage: build
  script:
    - cd spotforus/
    - ./gradlew build -x test
    - ./gradlew.bat --stop

test:
  stage: test
  script:
    - cd spotforus/
    - ./gradlew test
    - ./gradlew.bat --stop